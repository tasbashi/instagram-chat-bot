# ══════════════════════════════════════════════════════════════
# Production Environment Template
# Copy to backend/.env and fill in values
# ══════════════════════════════════════════════════════════════

# ── Database (handled by docker-compose, rarely need to change) ──
DATABASE_URL=postgresql+asyncpg://chatbot_user:chatbot_secure_2026@postgres:5432/instagram_chatbot

# ── Qdrant (handled by docker-compose) ──
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# ── Instagram / Meta ──
VERIFY_TOKEN=instagram_webhook_verify
PAGE_ACCESS_TOKEN=
APP_SECRET=<your-app-secret>
NGROK_AUTH_TOKEN=<your-ngrok-auth-token>
FACEBOOK_APP_ID=<your-facebook-app-id>
FACEBOOK_APP_SECRET=<your-facebook-app-secret>
INSTAGRAM_APP_ID=<your-instagram-app-id>
INSTAGRAM_APP_SECRET=<your-instagram-app-secret>
BACKEND_URL=http://localhost:8000

# ── LLM Provider ──
LLM_PROVIDER=groq

# ── Groq ──
GROQ_API_KEY=<your-groq-api-key>
GROQ_MODEL=llama-3.3-70b-versatile

# ── Azure OpenAI (optional) ──
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_MODEL=gpt-4o
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# ── Vertex AI (Embeddings) ──
GOOGLE_CLOUD_PROJECT=<your-gcp-project-id>
GOOGLE_CLOUD_LOCATION=us-central1
EMBEDDING_MODEL=text-embedding-004
EMBEDDING_DIMENSION=768
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-key.json

# ── Email (SMTP) ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# ── App ──
PORT=8000
JWT_SECRET=<generate-a-strong-secret>
ENCRYPTION_KEY=<your-fernet-encryption-key>
FRONTEND_URL=http://63.182.241.50

# ── RAG ──
CHUNK_SIZE_TOKENS=400
CHUNK_OVERLAP_TOKENS=50
